/**
 * MEMBER 1 (Architect) - TASK 1: Sidebar Component
 * --------------------------------------------
 * 1. Build a professional Sidebar for navigation.
 * 2. It should accept 'role' to show different navigation links.
 * 3. Make it responsive (looks good on mobile and desktop).
 */

export default function Sidebar({ role }: { role: string }) {
  return null; // To be implemented by Member 1
}
